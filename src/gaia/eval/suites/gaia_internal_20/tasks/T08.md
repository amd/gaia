---
id: T08
title: Add a YAML-manifest agent `todo` via agents/registry.py (no code, pure declarative)
expected_fix_class: architectural
max_diff_loc: 30
max_wall_clock_min: 15
setup:
  required_checks:
    - "python -c 'from gaia.agents.registry import load_agent; load_agent(\"todo\")'"
scoring:
  - name: compiles
    check: "python -c 'from gaia.agents.registry import load_agent; load_agent(\"todo\")'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/ -x -k registry"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Architectural-class task —
register a new agent `todo` purely via YAML manifest (no `agent.py` subclass
needed) using the `KNOWN_TOOLS` map in `src/gaia/agents/registry.py`.
