---
id: T19
title: Merge-conflict-resolution — two branches both modified the same function
expected_fix_class: architectural
max_diff_loc: 80
max_wall_clock_min: 30
setup:
  required_checks:
    - "git status"
scoring:
  - name: compiles
    check: "python -c 'import gaia'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/ -x -k conflict"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Architectural-class task —
two branches both modified the same function in incompatible ways. The
agent must understand both intents, produce a merged version that
preserves both, and write a PR body explaining the reconciliation.
