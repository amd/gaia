---
id: T07
title: Given this seeded failing test, produce a patch
expected_fix_class: tool
max_diff_loc: 50
max_wall_clock_min: 15
setup:
  fixture: fixtures/T07.patch
  required_checks:
    - "pytest tests/unit/test_t07_seeded.py -x"
scoring:
  - name: compiles
    check: "python -c 'import tests.unit.test_t07_seeded'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/test_t07_seeded.py -x"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

The fixture at ``fixtures/T07.patch`` seeds a failing unit test and a
deliberate off-by-one in a helper function (``_trim_trailing_whitespace``
in ``src/gaia/utils/strings.py`` — a fresh file added by the fixture).

The test asserts ``_trim_trailing_whitespace("abc   \n")`` returns
``"abc\n"``. The seeded implementation returns ``"abc\n "`` (strips the
wrong side).

Fix the helper so the test passes.

## Required work

1. Read the failing test to confirm the expected behaviour.
2. Edit ``src/gaia/utils/strings.py`` — minimal change only.
3. Verify by running ``pytest tests/unit/test_t07_seeded.py -x`` locally.

## Acceptance

- ``pytest tests/unit/test_t07_seeded.py -x`` passes.
- No other tests regress (``pytest tests/unit/ -x`` stays green).
- Diff touches ONLY ``src/gaia/utils/strings.py``.

## Budget

Diff ≤ 50 LoC, ≤ 15 min wall clock.
