---
id: T12
title: Add a new skill skills/touching-ci-yaml.md with the right triggers in catalog.toml
expected_fix_class: prompt
max_diff_loc: 50
max_wall_clock_min: 20
setup:
  required_checks:
    - "test -f src/gaia/coder/skills/touching-ci-yaml.md"
scoring:
  - name: compiles
    check: "test -f src/gaia/coder/skills/touching-ci-yaml.md"
    weight: 0.2
  - name: tests_pass
    check: "grep -q 'touching-ci-yaml' src/gaia/coder/skills/catalog.toml"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Prompt-class task — add a
new skill file with SKILL-format front-matter, register it in
`catalog.toml` with trigger patterns matching CI YAML changes.
