---
id: T13
title: Bump loop_version and add a new security_review state between verify_local and self_review
expected_fix_class: state-machine
max_diff_loc: 80
max_wall_clock_min: 30
setup:
  required_checks:
    - "python -c 'from gaia.coder.loop import DEFAULT_LOOP'"
scoring:
  - name: compiles
    check: "python -c 'from gaia.coder.loop import DEFAULT_LOOP'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/ -x -k loop"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. State-machine-class task —
this is one of the most sensitive self-edit cases. The agent must bump
`loop_version`, add a new `security_review` state, wire transitions, and
cite the state-machine edit cool-down rule (§11 open question #8).
