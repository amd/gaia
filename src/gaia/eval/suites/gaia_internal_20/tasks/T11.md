---
id: T11
title: Update GAIA.md to add a new working-style rule; remove an obsolete one (net-zero)
expected_fix_class: prompt
max_diff_loc: 20
max_wall_clock_min: 15
setup:
  required_checks:
    - "test -f src/gaia/coder/GAIA.md"
scoring:
  - name: compiles
    check: "test -f src/gaia/coder/GAIA.md"
    weight: 0.2
  - name: tests_pass
    check: "grep -q 'Working-style' src/gaia/coder/GAIA.md"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Prompt-class task —
net-zero edit to `src/gaia/coder/GAIA.md` (add one rule, remove one
obsolete rule). PR body must call out the deletion explicitly.
