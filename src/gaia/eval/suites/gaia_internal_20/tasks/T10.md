---
id: T10
title: Write a property-based test for generate_diff covering 20 randomised inputs
expected_fix_class: test
max_diff_loc: 60
max_wall_clock_min: 20
setup:
  required_checks:
    - "pytest tests/unit/test_generate_diff_properties.py -x"
scoring:
  - name: compiles
    check: "python -c 'import tests.unit.test_generate_diff_properties'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/test_generate_diff_properties.py -x"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Test-class task — use
`hypothesis` to generate 20 randomised inputs for `generate_diff`, assert
round-trip properties (apply(diff(A, B)) == B, diff(A, A) is empty, etc.).
