---
id: T06
title: Triage the last CI failure on main and open a diagnosis issue
expected_fix_class: policy
max_diff_loc: 20
max_wall_clock_min: 15
setup:
  required_checks: []
scoring:
  - name: compiles
    check: "true"
    weight: 0.2
  - name: tests_pass
    check: "test -f .eval-artifacts/T06/diagnosis.md || test -f diagnosis.md"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Policy-class task — under
`--no-network-writes`, the agent writes a `diagnosis.md` artifact summarising
the most recent CI failure on `main` (job name, failing step, suspected
root cause, proposed fix) **instead of** actually opening a GitHub issue.
The full brief names specific guardrail paths.
