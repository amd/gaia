---
id: T04
title: Migrate ChatAgent references to GaiaAgent under src/gaia/apps/webui/
expected_fix_class: architectural
max_diff_loc: 400
max_wall_clock_min: 30
setup:
  required_checks:
    - "pytest tests/unit/ -x -k webui"
    - "python util/lint.py --all"
scoring:
  - name: compiles
    check: "python -c 'from gaia.apps.webui import *'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/ -x -k webui"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder authored during Phase 2 eval-harness
scaffolding. See §10.2 of `docs/plans/coder-agent.mdx` for the full brief:

> Migrate `ChatAgent` → `GaiaAgent` under `src/gaia/apps/webui/`. All
> symbols in that tree should use the new name, imports and tests green.
> Budget: 400 LoC, architectural.

The front-matter is authoritative for the harness; the prose body will be
fleshed out when T04 becomes a primary test case.
