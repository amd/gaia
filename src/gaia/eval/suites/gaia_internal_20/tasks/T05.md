---
id: T05
title: Update docs/guides/code.mdx to match the current source
expected_fix_class: doc
max_diff_loc: 150
max_wall_clock_min: 20
setup:
  required_checks:
    - "test -f docs/guides/code.mdx"
scoring:
  - name: compiles
    check: "test -f docs/guides/code.mdx"
    weight: 0.2
  - name: tests_pass
    check: "grep -q 'gaia-code' docs/guides/code.mdx"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Doc-class task — update
`docs/guides/code.mdx` to reflect the current CodeAgent implementation
(correct CLI flags, default model, link targets).
