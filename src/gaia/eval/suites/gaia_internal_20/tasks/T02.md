---
id: T02
title: Fix lint errors in src/gaia/agents/chat/agent.py
expected_fix_class: test
max_diff_loc: 40
max_wall_clock_min: 10
setup:
  required_checks:
    - "python util/lint.py --black --isort src/gaia/agents/chat/agent.py"
scoring:
  - name: compiles
    check: "python -c 'from gaia.agents.chat.agent import ChatAgent'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/ -x -k chat"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

``src/gaia/agents/chat/agent.py`` has accumulated lint warnings flagged by
``python util/lint.py --all`` — unused imports, inconsistent quote style,
and at least one trailing-whitespace offender.

Fix the lint errors **without changing behaviour**. If ``util/lint.py --fix``
covers the whole diff, just run it and commit. If any warnings remain after
auto-fix (rare but possible for line-length issues), fix them by hand with
minimal changes.

## Acceptance

- ``python util/lint.py --all`` passes with zero new errors.
- ``pytest tests/unit/ -x -k chat`` still passes (behaviour unchanged).
- The PR body states which lint rule(s) you fixed.
- Diff touches ONLY ``src/gaia/agents/chat/agent.py`` — no drive-by edits.

## Budget

Diff ≤ 40 LoC, ≤ 10 min wall clock.
