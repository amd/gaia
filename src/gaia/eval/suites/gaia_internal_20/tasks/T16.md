---
id: T16
title: Add a DatabaseMixin-backed CRUD helper for a new table `notes`
expected_fix_class: tool
max_diff_loc: 120
max_wall_clock_min: 25
setup:
  required_checks:
    - "python -c 'from gaia.database.notes import NotesStore'"
scoring:
  - name: compiles
    check: "python -c 'from gaia.database.notes import NotesStore'"
    weight: 0.2
  - name: tests_pass
    check: "pytest tests/unit/test_notes_store.py -x"
    weight: 0.4
  - name: no_lint_regression
    check: "python util/lint.py --all"
    weight: 0.2
  - name: pr_mergeable
    check: "diff_applies_to_coder_cleanly"
    weight: 0.2
---

# Task

TODO: expand description. Placeholder per §10.2. Tool-class task — add a
new table `notes` with create/read/update/delete helpers layered on
`DatabaseMixin`. DDL goes in a module-level constant; the helper exposes
idiomatic Python signatures that match the other stores.
