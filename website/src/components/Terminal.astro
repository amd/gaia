---
// Copyright(C) 2024-2026 Advanced Micro Devices, Inc. All rights reserved.
// SPDX-License-Identifier: MIT

// Hero terminal: a scripted email-agent session that types on character by
// character. The transcript is rendered server-side, so no-JS and
// prefers-reduced-motion visitors get the whole session statically.
//
// The session is ILLUSTRATIVE, not captured output — the commands, the stream
// event types (status / tool_call / final) and the triage categories are real,
// the message subjects are invented. That is why the window chrome says so.

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

// [text, class]. Classes are the .log-* / .tok-* spans scoped to .g-code-panel.
type Segment = [string, string];

const LINES: Segment[][] = [
  [['$ ', 'tok-key'], ['lemonade-server serve', '']],
  [['✦ Gemma-4-E4B-it-GGUF loaded', 'log-dim']],
  [['$ ', 'tok-key'], ['npx @amd-gaia/agent-email playground', '']],
  [['◆ sidecar up on 127.0.0.1:8131', 'tok-key']],
  [['› ', 'tok-key'], ["find today's urgent mail, archive promos", '']],
  [['[status] ', 'log-dim'], ['resolving mailbox · Google connector', '']],
  [['[tool_call] ', 'log-dim'], ['triage · classifying on-device', '']],
  [['  URGENT          ', 'log-urgent'], ['Prod incident follow-up — Sarah', '']],
  [['  NEEDS_RESPONSE  ', 'log-needs'], ['Contract redline due Fri — Legal', '']],
  [['  FYI             ', 'log-fyi'], ['Weekly metrics digest', 'log-dim']],
  [['  PROMOTIONAL     ', 'log-dim'], ['6 messages → archived', 'log-dim']],
  [['[tool_call] ', 'log-dim'], ['draft_reply · awaiting your confirmation', '']],
  [['[final] ', 'log-ok'], ['no email content left the device', 'log-ok']],
];

const escChar = (c: string) =>
  c === '&' ? '&amp;' : c === '<' ? '&lt;' : c === '>' ? '&gt;' : c;

// Leading spaces and space runs become &nbsp; so the severity column stays
// aligned; single interior spaces stay breakable so long lines still wrap.
const renderLine = (segments: Segment[]) => {
  let prev = '';
  return segments
    .map(([text, cls]) => {
      let out = '';
      for (const ch of text) {
        out += ch === ' ' && (prev === '' || prev === ' ') ? '&nbsp;' : escChar(ch);
        prev = ch;
      }
      return `<span class="${cls}">${out}</span>`;
    })
    .join('');
};

const transcript = `${LINES.map(renderLine).join('<br>')}<span class="tok-key">▊</span>`;
---

<div
  class:list={[
    'g-code-panel border-g-border2 shadow-g-terminal transition-all duration-[400ms] ease-g-out hover:-translate-y-1 hover:border-g-gold-dim hover:shadow-g-card',
    className,
  ]}
>
  <div class="g-code-chrome">
    <span class="h-[11px] w-[11px] rounded-full bg-white/10" aria-hidden="true"></span>
    <span class="h-[11px] w-[11px] rounded-full bg-white/10" aria-hidden="true"></span>
    <span class="h-[11px] w-[11px] rounded-full bg-white/10" aria-hidden="true"></span>
    <span class="mono ml-1.5 text-[11.5px] text-g-faint">agent-email v0.5.0 · illustrative</span>
    <span class="mono ml-auto rounded-g-badge border border-g-gold-dim px-1.5 py-0.5 text-[10px] uppercase tracking-[0.08em] text-g-gold">local</span>
  </div>

  <div
    data-terminal
    data-lines={JSON.stringify(LINES)}
    role="img"
    aria-label="Illustrative terminal session: the email agent triages an inbox on-device."
    class="mono h-[268px] overflow-y-auto px-[18px] pb-[22px] pt-[18px] text-[13px] leading-[1.85] text-g-code-text"
    set:html={transcript}
  />
</div>

<script>
  type Segment = [string, string];

  const CHAR_PROMPT_MS = 26;
  const CHAR_OUTPUT_MS = 12;
  const LINE_GAP_MS = 260;
  const RESTART_MS = 3800;

  const escChar = (c: string) =>
    c === '&' ? '&amp;' : c === '<' ? '&lt;' : c === '>' ? '&gt;' : c;

  document.querySelectorAll<HTMLElement>('[data-terminal]').forEach((el) => {
    // Reduced motion keeps the server-rendered transcript exactly as-is.
    if (matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    const lines: Segment[][] = JSON.parse(el.dataset.lines || '[]');
    if (!lines.length) return;

    let lineIdx = 0;
    let segIdx = 0;
    let charIdx = 0;
    let built = '';
    let prevChar = '';
    let timer = 0;

    const isPromptLine = (line: Segment[]) => {
      const head = line[0]?.[0] ?? '';
      return head.startsWith('$') || head.startsWith('›');
    };

    const paint = () => {
      el.innerHTML = `${built}<span class="tok-key">▊</span>`;
      el.scrollTop = el.scrollHeight;
    };

    const tick = () => {
      if (lineIdx >= lines.length) {
        timer = window.setTimeout(() => {
          lineIdx = 0;
          segIdx = 0;
          charIdx = 0;
          built = '';
          prevChar = '';
          tick();
        }, RESTART_MS);
        return;
      }

      const line = lines[lineIdx];

      if (segIdx >= line.length) {
        built += '<br>';
        lineIdx += 1;
        segIdx = 0;
        charIdx = 0;
        prevChar = '';
        paint();
        timer = window.setTimeout(tick, LINE_GAP_MS);
        return;
      }

      const [text, cls] = line[segIdx];
      if (charIdx === 0) built += `<span class="${cls}">`;

      if (charIdx < text.length) {
        const ch = text[charIdx];
        built += ch === ' ' && (prevChar === '' || prevChar === ' ') ? '&nbsp;' : escChar(ch);
        prevChar = ch;
        charIdx += 1;
        paint();
        timer = window.setTimeout(tick, isPromptLine(line) ? CHAR_PROMPT_MS : CHAR_OUTPUT_MS);
        return;
      }

      built += '</span>';
      segIdx += 1;
      charIdx = 0;
      tick();
    };

    built = '';
    paint();
    timer = window.setTimeout(tick, 600);

    window.addEventListener('pagehide', () => clearTimeout(timer));
  });
</script>
