{
  "_comment": "Quality-gate thresholds for the #1230 email-triage corpus (#1278). The bars (fp_max/fn_max) are the AC targets; 'enforce' is the SINGLE switch CI keys off. It ships FALSE (report mode) on purpose: the FP<5%/FN<2% bars only become meaningful once categorization accuracy lands (#1266; current corpus accuracy ~0.40). #1112 (CI) and #1266 flip 'enforce' to true here — in data, not code — to make the harness fail on a breach. Loaded by gaia.eval.quality_metrics.load_quality_thresholds.",
  "fp_max": 0.05,
  "fn_max": 0.02,
  "axis": "needs_attention",
  "enforce": false,
  "_acceptance_comment": "Release-gate acceptance bar (#1437): the per-version scorecard gate (gaia.eval.scorecard_gate) enforces 'acceptance_target' as --min-aggregate (on the within-one-bucket aggregate, ×100) and 'urgent_recall_floor' as --min-urgent-recall (anti-gaming). 'acceptance_enforce' is the switch the release_agent_email.yml gate keys off: when true it passes the bar/floor flags so a sub-bar candidate blocks; when false it runs report-only. These keys are read by the workflow, not by load_quality_thresholds (which ignores extras). RE-MEASURED on the vendor-derived 249-email corpus, full-corpus scoring, Strix Halo / Gemma-4-E4B, 3 runs: within-one acceptance 0.834 (stdev 0.0116) >= 0.80 target; urgent_recall 0.9938 (per-run [0.9907, 1.0, 0.9907], stdev 0.0054). urgent_recall_floor raised 0.65 -> 0.90: the vendor corpus's needs-attention recall is near-ceiling without help, so the floor is locked ~0.09 below the measured min-run (0.9907) — tight because the metric is near-deterministic here (small stdev), loose enough to absorb run-to-run jitter. It is a floor, NOT an aspiration; raise it as the agent's urgent recall improves. (The #1903 escalation levers, tuned on the prior synthetic corpus, were reverted from this corpus: they regressed within-one 0.834->0.797 and needs-attention FPR 0.376->0.485 while improving nothing.)",
  "acceptance_target": 0.80,
  "urgent_recall_floor": 0.90,
  "acceptance_enforce": true,
  "_ctx_comment": "Ctx-window envelope (#1892): when require_ctx_match is true, release_agent_email.yml passes --require-ctx-match to the scorecard gate so a candidate SCORECARD.md that does not record recipe.environment.ctx_size (the window it was measured under) hard-fails. Same data-not-code switch pattern as acceptance_enforce. Ships FALSE for now: no committed SCORECARD carries a ctx stamp yet, so enforcing would break CI on merge. Flip to true when the consolidated eval pass (#1319/#1892) commits the first ctx-stamped SCORECARD — email_scorecard_refresh.yml already pins --ctx-size 16384, so the next organic refresh produces a stamped card.",
  "require_ctx_match": false
}
