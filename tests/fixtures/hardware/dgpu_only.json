{
  "_comment": "Legacy/synthetic shape (dict-keyed amd_dgpu) — real Lemonade 11 reports amd_gpu as a list. Kept only for the ensure_ready tier-comparison tests that consume it; see the lemonade11_*.json fixtures for real payload shapes.",
  "devices": {
    "amd_dgpu": { "memory_mb": 16384, "name": "AMD Radeon RX 7900 XTX" },
    "cpu": { "cores": 16 }
  }
}
