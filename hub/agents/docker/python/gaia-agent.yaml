id: docker
name: Docker
version: 0.1.0
description: "Container management — build, run, and inspect Docker containers"
author: AMD
license: MIT

category: infrastructure
tags: [docker, containers, devops]
icon: container
tools_count: 0

language: python
min_gaia_version: "0.20.0"
models: [Gemma-4-E4B-it-GGUF]

python:
  entry_module: gaia_agent_docker
  entry_class: DockerAgent
  dependencies:
    - "amd-gaia>=0.20.0"

requirements:
  min_memory_gb: 8
  platforms: [win-x64, linux-x64, darwin-arm64]

interfaces:
  tui: false
  cli: true
  pipe: true
  api_server: true
  mcp_server: true
